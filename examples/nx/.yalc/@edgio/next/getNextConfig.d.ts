/**
 * Gets the data in next.config.js
 * @param appDir The root directory of the app, defauts to the current working directory
 * @returns
 */
export default function getNextConfig(appDir?: string): any;
