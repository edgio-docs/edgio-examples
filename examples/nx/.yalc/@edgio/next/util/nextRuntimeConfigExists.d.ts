/**
 * Returns true if the runtime version of next.config.js file already exists
 * @param dir Working directory
 * @returns
 */
export default function nextRuntimeConfigExists(dir?: string): boolean;
